# Assets

Place textures, models, and other media files here.

Future additions:
- `plastic-normal.jpg` - subtle surface wear normal map
- `lcd-noise.png` - LCD pixel grid texture
- `vmu.glb` - optional high-fidelity 3D model

About intercom portrait:
- `intercom-portrait.gif` - used by the ABOUT codec panel (swap path in `js/intercom.js` if needed)
